<% content_for :full_height, true %>
<iframe
  src="http://<%= request.host %>:8092"
  class="flex-1 w-full border-none"
  style="height: calc(100vh - 120px)"
  title="Agenda – Nextcloud"
  allow="fullscreen"
></iframe>
